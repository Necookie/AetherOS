{
  "name": "aetheros-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.11.0",
  "engines": {
    "node": ">=20.19.0 <25",
    "npm": ">=10"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "typecheck": "npm run typecheck:functions && npm run typecheck --workspace=client && npm run typecheck --workspace=server",
    "typecheck:functions": "tsc --noEmit -p functions/tsconfig.json",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "build:cloudflare": "npm run build --workspace=client",
    "dev:cloudflare": "npm run build:cloudflare && wrangler pages dev client/dist",
    "deploy:cloudflare": "npm run build:cloudflare && wrangler pages deploy client/dist --project-name aetheros --commit-dirty=true",
    "lint": "npm run lint:functions && npm run lint --workspace=client && npm run lint --workspace=server",
    "lint:functions": "eslint functions --ext .ts --report-unused-disable-directives --max-warnings 0",
    "test": "npm run test --workspace=client"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "concurrently": "^8.2.2",
    "wrangler": "^4.131.0"
  }
}
