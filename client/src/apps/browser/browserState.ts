import {
    createTabId,
    createTabState,
} from './domains/tabs/tabDomain';
import { createHistoryEntry } from './domains/history/historyDomain';

export { createTabId, createTabState, createHistoryEntry };
